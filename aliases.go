package umbu

import (
	html "html/template"
)

type (
	CSS      = html.CSS
	HTML     = html.HTML
	HTMLAttr = html.HTMLAttr
	JS       = html.JS
	JSStr    = html.JSStr
	URL      = html.URL
	Srcset   = html.Srcset
)

var (
	HTMLEscapeString = html.HTMLEscapeString
)
